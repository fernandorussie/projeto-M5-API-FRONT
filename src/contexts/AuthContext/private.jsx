// import React, {useContext} from 'react';
// import { AuthContext } from './index';

// const {signed, setSigned, signedAdm, setSignedAdm} = useContext(AuthContext);
// export const Private = ({Item}) => {

//     return signed > 0 ? <Item/> : <Login/>
//   }
// export const PrivateAdm = ({Item}) => {

// return signedAdm > 0 ? <Item/> : <Login/>
// }